import React from "react";
import {AppProps} from "../../../../App";
import styled from "styled-components";

export default function ProfileScreen({darkMode, setDarkMode}: AppProps) {
  return (
      <Wrapper>
        ProfileScreen
      </Wrapper>
  );
}

const Wrapper = styled.div`
    
`
